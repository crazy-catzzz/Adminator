include ./includes/mixins

doctype
html(lang="en")
  head
    include includes/header.pug
body(style='background-color: black;')
  include includes/navbar.pug
  include includes/jumbotronv4.pug
  .container
    .jumbotron.text-center.bg-transparent
      h1(style='color: #ffc107;').display-2 Commands
      p(style='color: #00cc00;').lead Discover everything Adminator has.
      hr(style='color: white;')
    
    section#commands
      .row
        .col-sm-3
          each category in categories
            +category(category)
        .col-sm-9
          ul.list-group.mb-2
            li.list-group-item test
